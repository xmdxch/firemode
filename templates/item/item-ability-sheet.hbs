<form class="{{cssClass}}" autocomplete="off">
  {{!-- Header --}}
  <header class="sheet-header">
    <img class="profile-img" src="{{item.img}}" data-edit="img" title="{{item.name}}" />
    <div class="header-fields">
      <h1 class="charname"><input name="name" type="text" value="{{item.name}}" placeholder="Name" /></h1>
    </div>
    <div class="item-subtitle">
      <h4 class="item-type">{{itemType}}</h4>
    </div>
  </header>

  {{!-- Navigation --}}
  <nav class="sheet-navigation tabs" data-group="primary">
    <a class="item active" data-tab="description">{{ localize "HTBAH.Description" }}</a>
  </nav>

  {{!-- Sheet Body --}}
  <section class="sheet-body">
    <div class="sidebar">
      <div class="sidebar-card">
        <h3>Item Details</h3>
        <div class="value-field">
          <label class="resource-label">{{localize 'Skillset'}}</label>
          <select name="system.skillSet">
            {{#each config.skillSets as |set key|}}
              <option value="{{key}}" {{#if (eq ../system.skillSet key)}}selected{{/if}}>
                {{localize set.label}}
              </option>
            {{/each}}
          </select>
        </div>
        <div class="value-field">
          <label for="item-value">Value:</label>
          <input id="item-value" name="system.value" type="number" value="{{item.system.value}}" placeholder="Enter value" />
        </div>
        <div class="calculated-value-field">
          <label for="item-calculated-value">Calculated Value:</label>
          <input id="item-calculated-value" type="text" value="{{item.system.calculatedValue}}" disabled />
        </div>
      </div>
    </div>

    <div class="main-content">
      <div class="tab description" data-group="primary" data-tab="description">
        <div class="editor">
          <editor-content name="system.description">{{item.system.description}}</editor-content>
        </div>
      </div>
    </div>
  </section>
</form>